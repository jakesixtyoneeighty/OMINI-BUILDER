#:schema node_modules/wrangler/config-schema.json
name = "bolt"
compatibility_flags = ["nodejs_compat_v2"]
compatibility_date = "2024-09-23"
pages_build_output_dir = "./build/client"

# Do NOT declare NETLIFY_DEFAULT_API_KEY here — it must be set in Cloudflare Pages dashboard.
# Declaring it with an empty value overrides the Pages environment variable.
# [vars]
# NETLIFY_DEFAULT_API_KEY = ""
